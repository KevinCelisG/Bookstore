package com.example.bookstore.domain.model

/**
 * Data class representing a book with a title and an image URL.
 * @property title The title of the book.
 * @property image The URL of the book's cover image.
 */
data class Book(val title: String, val image: String)